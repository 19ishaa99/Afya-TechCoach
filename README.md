# Afya TechCoach (UI scaffold)

This repository contains the Expo React Native UI scaffold for Afya TechCoach.

Run locally with Expo (install dependencies first):

```bash
npm install
npx expo start
```

Notes:
- This is UI-only. No backend or API calls are implemented.
- Navigation uses React Navigation stack in `src/navigation/AuthStack.js`.
